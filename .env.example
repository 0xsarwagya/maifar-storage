DATABASE_URL=postgres://postgres:postgres@localhost:5432/maifar

# MQTT: set host + port (TLS on by default → mqtts + 8883), OR set MQTT_URL instead
MQTT_HOST=mqtt.maifar.actimi.com
MQTT_PORT=8883
# Plain MQTT: MQTT_SSL=false and e.g. MQTT_PORT=1883
# MQTT_SSL=false

# Alternative full URL (if set, MQTT_HOST / MQTT_PORT / MQTT_SSL are ignored)
# MQTT_URL=mqtts://mqtt.maifar.actimi.com:8883

MQTT_USERNAME=
MQTT_PASSWORD=
# MQTT_CLIENT_ID=maifar-storage-prod-1

MQTT_TOPICS=devices/+/telemetry,#

HTTP_PORT=3000
BATCH_MAX=100
FLUSH_INTERVAL_MS=1000

# DEVICE_ID_TOPIC_REGEX=^devices/([^/]+)/
# DEVICE_ID_JSON_KEY=deviceId
